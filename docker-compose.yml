services:
  postgres:
    image: postgres:17.5-alpine3.21
    restart: always
    environment:
      POSTGRES_USER: db_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spring_commerce
    ports:
      - "5432:5432"